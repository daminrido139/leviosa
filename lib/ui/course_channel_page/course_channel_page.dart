import 'package:flutter/material.dart';

class CourseChannelPage extends StatelessWidget {
  const CourseChannelPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Course Channel Page'),
      ),
    );
  }
}
